package com.project.game.model.board;

/**
 * interfaccia che permette di creare il metodo
 * per il calcolo della distanza di Manhattan per ogni box
 */
public interface Manhattan {
    int getManhattan(int value, int index);
}
